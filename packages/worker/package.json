{
  "name": "@ai-builder/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute ai-builder-db --local --file=../../migrations/0001_init.sql",
    "db:migrate": "wrangler d1 execute ai-builder-db --remote --file=../../migrations/0001_init.sql",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240815.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.74.0"
  }
}
