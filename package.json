{
  "name": "ai-builder",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/worker",
    "deploy": "npm run deploy --workspace=packages/worker",
    "type-check": "npm run type-check --workspace=packages/worker"
  }
}
